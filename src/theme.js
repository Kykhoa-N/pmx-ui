import { createTheme } from "@mui/material";

export const appTheme = createTheme({
  palette: {
    background: {
      default: "#EFF0F475",
    },
    primary: {
      main: "#323D72",
    },
    chatbot: {
      title: "#4ade80",
    },
  },
});
